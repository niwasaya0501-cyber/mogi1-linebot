# LINE bot + AI 自動応答（GAS＋スプレッドシート版）ヒアリング

Vercel＋Supabase版（[requirements.md](./requirements.md)）と同じ依頼内容の廉価版という位置づけ。既存の検討メモ（requirements.md 7章）を踏まえ、実際にヒアリングを行った内容。

## 1. 現状の課題
LINEで予約の問い合わせが多くて対応が大変。営業時間・メニュー・空き状況の質問対応に追われている。予算はあまりなく、月額もなるべく安くしたい。1日10〜15件の問い合わせ、土日はもう少し多い。よくある質問は「営業時間は？」「カットの値段は？」「予約取れますか？」「パーマとカラー同時にできる？」「駐車場ある？」。

## 前提・制約
- 以前Vercel＋Supabaseで作った同種のLINEボット案件と同じ依頼内容の廉価版という位置づけ（今回はGAS＋スプレッドシートのみで構築し、月額費用ゼロを目指す）
- スマホで更新できる管理画面は今回は作らない（GAS単体で完結、Vercelは使わない）
- LINE公式アカウントは既に保有、友だち約300人
- 予算は初期10万円以内、月額はなるべく安く

## 要件
- まずは質問応答（AIによる自動回答）だけで良い。将来的に予約機能も検討したいが今回はスコープ外
- 料金は半年に1回程度変更があるため、コード変更なしでオーナー自身が更新できる形にしたい（スプレッドシート編集で反映）
- AIが答えられない質問はオーナーに通知してほしい（LINE通知）
- 臨時休業などのお知らせをお客さん全員にLINEで一斉送信できるようにしたい（オーナーがコードを触らず操作できる形）

## 技術方針（決定済み）
- GAS（Google Apps Script）でLINE Webhook受信・AI API呼び出し・スプレッドシート書き込み・LINE返信/通知/一斉送信を全て実装
- データ保存はGoogleスプレッドシート（FAQ・問い合わせログ・料金表など）
- データ削除・保持期間のポリシーはまだ決めない。実装後に実際の応答速度を計測し、5秒以内の応答を目安に、遅くなるようなら古い履歴を間引く対応を検討する
- サーバー費用・ホスティング費用ゼロ。変動費はAI API利用料のみ（1日10〜15件規模なら少額の見込み）
